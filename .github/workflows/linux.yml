name: Package for Linux

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Package Application
      uses: tevenfeng/Pyinstaller-docker@main
      with:
        path: .

    - uses: actions/upload-artifact@v2
      with:
        name: winfred_linux
        path: dist/linux